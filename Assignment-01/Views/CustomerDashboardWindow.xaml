<Window x:Class="Assignment_01.Views.CustomerDashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="FUMiniHotelSystem - Customer Dashboard" 
        Height="600" 
        Width="900"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#444444"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#333333" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="FUMiniHotelSystem - Customer Dashboard" 
                              FontSize="20" 
                              FontWeight="Bold" 
                              Foreground="White"/>
                    <TextBlock Text="{Binding WelcomeMessage}" 
                              FontSize="12" 
                              Foreground="White" 
                              Margin="0,5,0,0"/>
                </StackPanel>
                
                <Button Grid.Column="1" 
                       Content="Đăng xuất" 
                       Click="LogoutButton_Click"
                       Background="#E74C3C"/>
            </Grid>
        </Border>

        <!-- Main Content -->
        <TabControl Grid.Row="1" Margin="10">
            <!-- Profile Management Tab -->
            <TabItem Header="Quản lý Hồ sơ">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="Thông tin cá nhân" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
                    
                    <Grid Grid.Row="1" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Họ và tên:" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding Customer.CustomerFullName}" IsReadOnly="True"/>
                    </Grid>
                    
                    <Grid Grid.Row="2" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Email:" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding Customer.EmailAddress}" IsReadOnly="True"/>
                    </Grid>
                    
                    <Grid Grid.Row="3" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Số điện thoại:" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding Customer.Telephone}"/>
                    </Grid>
                    
                    <Grid Grid.Row="4" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Ngày sinh:" VerticalAlignment="Center"/>
                        <DatePicker Grid.Column="1" SelectedDate="{Binding Customer.CustomerBirthday}"/>
                    </Grid>
                    
                    <Grid Grid.Row="5" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Mật khẩu mới:" VerticalAlignment="Center"/>
                        <PasswordBox Grid.Column="1" x:Name="NewPasswordBox"/>
                    </Grid>
                    
                    <Grid Grid.Row="6" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Xác nhận mật khẩu:" VerticalAlignment="Center"/>
                        <PasswordBox Grid.Column="1" x:Name="ConfirmPasswordBox"/>
                    </Grid>
                    
                    <StackPanel Grid.Row="7" Orientation="Horizontal">
                        <Button Content="Cập nhật thông tin" Click="UpdateProfile_Click"/>
                        <Button Content="Đổi mật khẩu" Click="ChangePassword_Click"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- Booking History Tab -->
            <TabItem Header="Lịch sử Đặt phòng">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Booking Controls -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="Đặt phòng mới" Click="NewBooking_Click"/>
                        <Button Content="Xem chi tiết" Click="ViewBookingDetails_Click"/>
                        <Button Content="Hủy đặt phòng" Click="CancelBooking_Click"/>
                    </StackPanel>
                    
                    <!-- Booking History DataGrid -->
                    <DataGrid Grid.Row="1" 
                             x:Name="BookingHistoryDataGrid"
                             ItemsSource="{Binding BookingHistory}"
                             SelectedItem="{Binding SelectedBooking}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding BookingID}" Width="50"/>
                            <DataGridTextColumn Header="Phòng" Binding="{Binding RoomInformation.RoomNumber}" Width="80"/>
                            <DataGridTextColumn Header="Loại phòng" Binding="{Binding RoomInformation.RoomType.RoomTypeName}" Width="100"/>
                            <DataGridTextColumn Header="Ngày đặt" Binding="{Binding BookingDate, StringFormat=dd/MM/yyyy}" Width="100"/>
                            <DataGridTextColumn Header="Check-in" Binding="{Binding CheckInDate, StringFormat=dd/MM/yyyy}" Width="100"/>
                            <DataGridTextColumn Header="Check-out" Binding="{Binding CheckOutDate, StringFormat=dd/MM/yyyy}" Width="100"/>
                            <DataGridTextColumn Header="Tổng tiền" Binding="{Binding TotalAmount, StringFormat={}{0:N0} VNĐ}" Width="120"/>
                            <DataGridTextColumn Header="Trạng thái" Binding="{Binding BookingStatus}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- New Booking Tab -->
            <TabItem Header="Đặt phòng mới">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="Đặt phòng mới" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
                    
                    <Grid Grid.Row="1" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Ngày check-in:" VerticalAlignment="Center"/>
                        <DatePicker Grid.Column="1" x:Name="CheckInDatePicker" SelectedDate="{Binding NewBooking.CheckInDate}"/>
                    </Grid>
                    
                    <Grid Grid.Row="2" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Ngày check-out:" VerticalAlignment="Center"/>
                        <DatePicker Grid.Column="1" x:Name="CheckOutDatePicker" SelectedDate="{Binding NewBooking.CheckOutDate}"/>
                    </Grid>
                    
                    <Grid Grid.Row="3" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Loại phòng:" VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1" x:Name="RoomTypeComboBox" 
                                 ItemsSource="{Binding RoomTypes}"
                                 DisplayMemberPath="RoomTypeName"
                                 SelectedValuePath="RoomTypeID"
                                 SelectedValue="{Binding SelectedRoomTypeId}"/>
                    </Grid>
                    
                    <Button Grid.Row="4" Content="Tìm phòng khả dụng" Click="SearchAvailableRooms_Click" Margin="0,10,0,20"/>
                    
                    <TextBlock Grid.Row="5" Text="Phòng khả dụng:" FontWeight="Bold" Margin="0,0,0,10"/>
                    
                    <DataGrid Grid.Row="6" 
                             x:Name="AvailableRoomsDataGrid"
                             ItemsSource="{Binding AvailableRooms}"
                             SelectedItem="{Binding SelectedAvailableRoom}"
                             Height="150">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Số phòng" Binding="{Binding RoomNumber}" Width="80"/>
                            <DataGridTextColumn Header="Mô tả" Binding="{Binding RoomDescription}" Width="*"/>
                            <DataGridTextColumn Header="Sức chứa" Binding="{Binding RoomMaxCapacity}" Width="80"/>
                            <DataGridTextColumn Header="Giá/ngày" Binding="{Binding RoomPricePerDate, StringFormat={}{0:N0} VNĐ}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <Grid Grid.Row="7" Margin="0,20,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Tổng tiền:" VerticalAlignment="Center" FontWeight="Bold"/>
                        <TextBlock Grid.Column="1" Text="{Binding TotalAmount, StringFormat={}{0:N0} VNĐ}" 
                                 FontWeight="Bold" FontSize="16" Foreground="#007ACC"/>
                    </Grid>
                    
                    <Button Grid.Row="8" Content="Xác nhận đặt phòng" Click="ConfirmBooking_Click"/>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2" Background="#1A1A1A">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="{Binding CurrentTime}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>

