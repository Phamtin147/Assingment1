<Window x:Class="Assignment_01.Views.BookingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Đặt phòng mới" 
        Height="600" 
        Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <Style TargetType="DatePicker">
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Background" Value="#404040"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#555555"/>
        </Style>
        
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0,5,0,10"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Background" Value="#404040"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#555555"/>
        </Style>
        
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        
        <Style x:Key="SearchButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#444444"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="ConfirmButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#27AE60"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2ECC71"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="CancelButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#555555"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#666666"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
        
        <Style x:Key="ErrorTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FF6B6B"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,2,0,5"/>
        </Style>
        
        <Style TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                  Text="Đặt phòng mới" 
                  FontSize="18" 
                  FontWeight="Bold" 
                  HorizontalAlignment="Center" 
                  Margin="0,0,0,20"/>

        <!-- Date Selection -->
        <GroupBox Grid.Row="1" Header="Chọn ngày" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Ngày check-in *"/>
                    <DatePicker x:Name="CheckInDatePicker" 
                               SelectedDate="{Binding CheckInDate}"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Margin="10,0,10,0">
                    <TextBlock Text="Ngày check-out *"/>
                    <DatePicker x:Name="CheckOutDatePicker" 
                               SelectedDate="{Binding CheckOutDate}"/>
                </StackPanel>
                
                <Button Grid.Column="2" 
                       Content="Tìm phòng" 
                       Style="{StaticResource SearchButtonStyle}"
                       Command="{Binding SearchRoomsCommand}"
                       VerticalAlignment="Bottom"
                       IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"/>
            </Grid>
        </GroupBox>

        <!-- Room Type Filter -->
        <GroupBox Grid.Row="2" Header="Lọc theo loại phòng" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <ComboBox Grid.Column="0" 
                         ItemsSource="{Binding RoomTypes}"
                         DisplayMemberPath="RoomTypeName"
                         SelectedValuePath="RoomTypeID"
                         SelectedValue="{Binding SelectedRoomTypeId}"
                         Margin="0,0,10,0"/>
                
                <Button Grid.Column="1" 
                       Content="Lọc" 
                       Style="{StaticResource SearchButtonStyle}"
                       Command="{Binding FilterRoomsCommand}"
                       IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"/>
            </Grid>
        </GroupBox>

        <!-- Available Rooms -->
        <GroupBox Grid.Row="3" Header="Phòng khả dụng">
            <Grid Margin="10">
                <DataGrid ItemsSource="{Binding AvailableRooms}"
                         SelectedItem="{Binding SelectedRoom}"
                         Height="200">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Số phòng" Binding="{Binding RoomNumber}" Width="80"/>
                        <DataGridTextColumn Header="Mô tả" Binding="{Binding RoomDescription}" Width="*"/>
                        <DataGridTextColumn Header="Loại" Binding="{Binding RoomType.RoomTypeName}" Width="100"/>
                        <DataGridTextColumn Header="Sức chứa" Binding="{Binding RoomMaxCapacity}" Width="80"/>
                        <DataGridTextColumn Header="Giá/ngày" Binding="{Binding RoomPricePerDate, StringFormat={}{0:N0} VNĐ}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>

        <!-- Booking Summary -->
        <GroupBox Grid.Row="4" Header="Tóm tắt đặt phòng" Margin="0,10,0,10">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <Grid Grid.Row="0" Margin="0,0,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Phòng đã chọn:"/>
                    <TextBlock Grid.Column="1" Text="{Binding SelectedRoom.RoomNumber}" FontWeight="Bold"/>
                </Grid>
                
                <Grid Grid.Row="1" Margin="0,0,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Số đêm:"/>
                    <TextBlock Grid.Column="1" Text="{Binding NumberOfNights}" FontWeight="Bold"/>
                </Grid>
                
                <Grid Grid.Row="2" Margin="0,0,0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Giá mỗi đêm:"/>
                    <TextBlock Grid.Column="1" Text="{Binding SelectedRoom.RoomPricePerDate, StringFormat={}{0:N0} VNĐ}" FontWeight="Bold"/>
                </Grid>
                
                <Grid Grid.Row="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Tổng tiền:" FontWeight="Bold" FontSize="14"/>
                    <TextBlock Grid.Column="1" Text="{Binding TotalAmount, StringFormat={}{0:N0} VNĐ}" 
                             FontWeight="Bold" FontSize="16" Foreground="#007ACC"/>
                </Grid>
            </Grid>
        </GroupBox>

        <!-- Error Message -->
        <TextBlock Grid.Row="5" 
                  Text="{Binding ErrorMessage}" 
                  Style="{StaticResource ErrorTextStyle}"
                  Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"
                  Margin="0,0,0,10"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" 
                   Orientation="Horizontal" 
                   HorizontalAlignment="Right" 
                   Margin="0,20,0,0">
            <Button Content="Xác nhận đặt phòng" 
                   Style="{StaticResource ConfirmButtonStyle}"
                   Command="{Binding ConfirmBookingCommand}"
                   IsEnabled="{Binding CanConfirmBooking}"/>
            <Button Content="Hủy" 
                   Style="{StaticResource CancelButtonStyle}"
                   Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
