<Window x:Class="Assignment_01.Views.AdminDashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="FUMiniHotelSystem - Admin Dashboard" 
        Height="600" 
        Width="1000"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#444444"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#333333" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="FUMiniHotelSystem - Admin Dashboard" 
                              FontSize="20" 
                              FontWeight="Bold" 
                              Foreground="White"/>
                    <TextBlock Text="{Binding WelcomeMessage}" 
                              FontSize="12" 
                              Foreground="White" 
                              Margin="0,5,0,0"/>
                </StackPanel>
                
                <Button Grid.Column="1" 
                       Content="Đăng xuất" 
                       Click="LogoutButton_Click"
                       Background="#E74C3C"/>
            </Grid>
        </Border>

        <!-- Main Content -->
        <TabControl Grid.Row="1" Margin="10">
            <!-- Customer Management Tab -->
            <TabItem Header="Quản lý Khách hàng">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Customer Controls -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="Thêm khách hàng" Click="AddCustomer_Click"/>
                        <Button Content="Sửa khách hàng" Click="EditCustomer_Click"/>
                        <Button Content="Xóa khách hàng" Click="DeleteCustomer_Click"/>
                        <TextBox x:Name="CustomerSearchBox" 
                                Width="200" 
                                Margin="20,0,0,0"
                                TextChanged="CustomerSearchBox_TextChanged"
                                Tag="Tìm kiếm khách hàng..."/>
                    </StackPanel>
                    
                    <!-- Customer DataGrid -->
                    <DataGrid Grid.Row="1" 
                             x:Name="CustomerDataGrid"
                             ItemsSource="{Binding Customers}"
                             SelectedItem="{Binding SelectedCustomer}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding CustomerID}" Width="50"/>
                            <DataGridTextColumn Header="Họ tên" Binding="{Binding CustomerFullName}" Width="*"/>
                            <DataGridTextColumn Header="Email" Binding="{Binding EmailAddress}" Width="200"/>
                            <DataGridTextColumn Header="Số điện thoại" Binding="{Binding Telephone}" Width="120"/>
                            <DataGridTextColumn Header="Ngày sinh" Binding="{Binding CustomerBirthday, StringFormat=dd/MM/yyyy}" Width="100"/>
                            <DataGridTextColumn Header="Trạng thái" Binding="{Binding CustomerStatus}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Room Management Tab -->
            <TabItem Header="Quản lý Phòng">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Room Controls -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="Thêm phòng" Click="AddRoom_Click"/>
                        <Button Content="Sửa phòng" Click="EditRoom_Click"/>
                        <Button Content="Xóa phòng" Click="DeleteRoom_Click"/>
                        <TextBox x:Name="RoomSearchBox" 
                                Width="200" 
                                Margin="20,0,0,0"
                                TextChanged="RoomSearchBox_TextChanged"
                                Tag="Tìm kiếm phòng..."/>
                    </StackPanel>
                    
                    <!-- Room DataGrid -->
                    <DataGrid Grid.Row="1" 
                             x:Name="RoomDataGrid"
                             ItemsSource="{Binding Rooms}"
                             SelectedItem="{Binding SelectedRoom}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding RoomID}" Width="50"/>
                            <DataGridTextColumn Header="Số phòng" Binding="{Binding RoomNumber}" Width="80"/>
                            <DataGridTextColumn Header="Mô tả" Binding="{Binding RoomDescription}" Width="*"/>
                            <DataGridTextColumn Header="Sức chứa" Binding="{Binding RoomMaxCapacity}" Width="80"/>
                            <DataGridTextColumn Header="Giá/ngày" Binding="{Binding RoomPricePerDate, StringFormat={}{0:N0} VNĐ}" Width="120"/>
                            <DataGridTextColumn Header="Loại phòng" Binding="{Binding RoomType.RoomTypeName}" Width="100"/>
                            <DataGridTextColumn Header="Trạng thái" Binding="{Binding RoomStatus}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Booking Management Tab -->
            <TabItem Header="Quản lý Đặt phòng">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Booking Controls -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="Tạo đặt phòng" Click="CreateBooking_Click"/>
                        <Button Content="Xem chi tiết" Click="ViewBooking_Click"/>
                        <Button Content="Hủy đặt phòng" Click="CancelBooking_Click"/>
                        <DatePicker x:Name="StartDatePicker" 
                                   Width="120" 
                                   Margin="20,0,5,0"
                                   SelectedDate="{Binding ReportStartDate}"/>
                        <DatePicker x:Name="EndDatePicker" 
                                   Width="120" 
                                   Margin="5,0,5,0"
                                   SelectedDate="{Binding ReportEndDate}"/>
                        <Button Content="Lọc theo ngày" Click="FilterBookings_Click"/>
                    </StackPanel>
                    
                    <!-- Booking DataGrid -->
                    <DataGrid Grid.Row="1" 
                             x:Name="BookingDataGrid"
                             ItemsSource="{Binding Bookings}"
                             SelectedItem="{Binding SelectedBooking}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding BookingID}" Width="50"/>
                            <DataGridTextColumn Header="Khách hàng" Binding="{Binding Customer.CustomerFullName}" Width="150"/>
                            <DataGridTextColumn Header="Phòng" Binding="{Binding RoomInformation.RoomNumber}" Width="80"/>
                            <DataGridTextColumn Header="Ngày đặt" Binding="{Binding BookingDate, StringFormat=dd/MM/yyyy}" Width="100"/>
                            <DataGridTextColumn Header="Check-in" Binding="{Binding CheckInDate, StringFormat=dd/MM/yyyy}" Width="100"/>
                            <DataGridTextColumn Header="Check-out" Binding="{Binding CheckOutDate, StringFormat=dd/MM/yyyy}" Width="100"/>
                            <DataGridTextColumn Header="Tổng tiền" Binding="{Binding TotalAmount, StringFormat={}{0:N0} VNĐ}" Width="120"/>
                            <DataGridTextColumn Header="Trạng thái" Binding="{Binding BookingStatus}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Reports Tab -->
            <TabItem Header="Báo cáo">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Report Controls -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <DatePicker x:Name="ReportStartDatePicker" 
                                   Width="120" 
                                   Margin="0,0,5,0"
                                   SelectedDate="{Binding ReportStartDate}"/>
                        <DatePicker x:Name="ReportEndDatePicker" 
                                   Width="120" 
                                   Margin="5,0,5,0"
                                   SelectedDate="{Binding ReportEndDate}"/>
                        <Button Content="Tạo báo cáo" Click="GenerateReport_Click"/>
                        <Button Content="Xuất Excel" Click="ExportReport_Click"/>
                    </StackPanel>
                    
                    <!-- Report DataGrid -->
                    <DataGrid Grid.Row="1" 
                             x:Name="ReportDataGrid"
                             ItemsSource="{Binding ReportData}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Ngày" Binding="{Binding Date, StringFormat=dd/MM/yyyy}" Width="100"/>
                            <DataGridTextColumn Header="Số đặt phòng" Binding="{Binding BookingCount}" Width="100"/>
                            <DataGridTextColumn Header="Doanh thu" Binding="{Binding Revenue, StringFormat={}{0:N0} VNĐ}" Width="150"/>
                            <DataGridTextColumn Header="Phòng phổ biến" Binding="{Binding PopularRoom}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2" Background="#1A1A1A">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="{Binding CurrentTime}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>

